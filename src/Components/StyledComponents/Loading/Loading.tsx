import * as React from "react";
import Styled from "./styles";

export const Loading = () => {
  return (
    <Styled.LoadingContainer>
      <Styled.Loading />
    </Styled.LoadingContainer>
  );
};
